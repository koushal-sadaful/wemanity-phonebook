import {PACKAGE_STATS_API_URL, SEARCH_SUGGESTION_URL} from "../config/URL";

export class UrlHelper {
    static getAllEntriesUrl = () => {
        return ""
    };

    static getSearchSuggestions = (queryString) => {
        return ""
    }

    static getExistingEntryData = (entryId) => {
        return ""
    }

}